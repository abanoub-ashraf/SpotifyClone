import UIKit

///
/// this enum is for laying out the indicator view that's under one of the buttons inside
/// the library toggle view under each button whenever any of them is clicked
///
enum State {
    case playlists
    case albums
}
